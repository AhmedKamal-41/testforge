[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers -m "not postgres"
markers =
    postgres: requires a running PostgreSQL via docker compose (opt-in: `pytest -m postgres`)
filterwarnings =
    ignore::DeprecationWarning
